---
apiVersion: v1
kind: Service
metadata:
  name: svc-{{ .Values.name }}
spec:
  selector:
    app: {{ .Values.name }}
  ports:
  - protocol: TCP
    port: {{ .Values.port }}
    targetPort: 25213
  type: NodePort